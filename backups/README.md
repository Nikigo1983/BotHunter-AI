# Database backups

SQL dumps created by `scripts/backup_database.ps1` or `scripts/backup_database.sh`.

**Not committed to git** — store copies off-site for disaster recovery.

See [docs/database.md](../docs/database.md#backup).
